{{#if d3el}}
  {{#with (compute callback d3el) as |data|}}
    {{yield data}}
    {{if on-enter (invoke (d3-enter on-enter) data)}}
    {{if on-exit (invoke (d3-exit on-exit) data)}}
  {{/with}}
{{/if}}