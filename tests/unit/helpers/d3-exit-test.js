import { d3Exit } from 'ember-d3-helpers/helpers/d3-exit';
import { module, test } from 'qunit';

module('Unit | Helper | d3 exit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Exit([42]);
  assert.ok(result);
});
