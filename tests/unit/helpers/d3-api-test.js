import { d3Api } from 'ember-d3-helpers/helpers/d3-api';
import { module, test } from 'qunit';

module('Unit | Helper | d3 api');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Api([42]);
  assert.ok(result);
});
