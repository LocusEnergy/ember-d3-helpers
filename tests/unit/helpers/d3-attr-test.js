import { d3Attr } from 'ember-d3-helpers/helpers/d3-attr';
import { module, test } from 'qunit';

module('Unit | Helper | d3 attr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Attr([42]);
  assert.ok(result);
});
