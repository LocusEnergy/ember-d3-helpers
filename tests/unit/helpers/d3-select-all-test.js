import { d3SelectAll } from 'ember-d3-helpers/helpers/d3-select-all';
import { module, test } from 'qunit';

module('Unit | Helper | d3 select all');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3SelectAll([42]);
  assert.ok(result);
});
