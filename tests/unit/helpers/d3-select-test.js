import { d3Select } from 'ember-d3-helpers/helpers/d3-select';
import { module, test } from 'qunit';

module('Unit | Helper | d3 select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Select([42]);
  assert.ok(result);
});
