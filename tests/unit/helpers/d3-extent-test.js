import { d3Extent } from 'ember-d3-helpers/helpers/d3-extent';
import { module, test } from 'qunit';

module('Unit | Helper | d3 extent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Extent([42]);
  assert.ok(result);
});
