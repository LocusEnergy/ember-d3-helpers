import { d3Enter } from 'ember-d3-helpers/helpers/d3-enter';
import { module, test } from 'qunit';

module('Unit | Helper | d3 enter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Enter([42]);
  assert.ok(result);
});
