import { d3Transition } from 'ember-d3-helpers/helpers/d3-transition';
import { module, test } from 'qunit';

module('Unit | Helper | d3 transition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Transition([42]);
  assert.ok(result);
});
