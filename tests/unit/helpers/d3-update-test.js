import { d3Update } from 'ember-d3-helpers/helpers/d3-update';
import { module, test } from 'qunit';

module('Unit | Helper | d3 update');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Update([42]);
  assert.ok(result);
});
