import { d3Remove } from 'ember-d3-helpers/helpers/d3-remove';
import { module, test } from 'qunit';

module('Unit | Helper | d3 remove');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Remove([42]);
  assert.ok(result);
});
