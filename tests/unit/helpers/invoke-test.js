import { invoke } from 'ember-d3-helpers/helpers/invoke';
import { module, test } from 'qunit';

module('Unit | Helper | invoke');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = invoke([42]);
  assert.ok(result);
});
