import { d3Delay } from 'ember-d3-helpers/helpers/d3-delay';
import { module, test } from 'qunit';

module('Unit | Helper | d3 delay');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Delay([42]);
  assert.ok(result);
});
