import { d3Data } from 'ember-d3-helpers/helpers/d3-data';
import { module, test } from 'qunit';

module('Unit | Helper | d3 data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Data([42]);
  assert.ok(result);
});
