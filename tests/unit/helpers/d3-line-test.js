import { d3Line } from 'ember-d3-helpers/helpers/d3-line';
import { module, test } from 'qunit';

module('Unit | Helper | d3 line');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Line([42]);
  assert.ok(result);
});
