import { d3Append } from 'ember-d3-helpers/helpers/d3-append';
import { module, test } from 'qunit';

module('Unit | Helper | d3 append');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Append([42]);
  assert.ok(result);
});
