import { d3Callback } from 'ember-d3-helpers/helpers/d3-callback';
import { module, test } from 'qunit';

module('Unit | Helper | d3 callback');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Callback([42]);
  assert.ok(result);
});
