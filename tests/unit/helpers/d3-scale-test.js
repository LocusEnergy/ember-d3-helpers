import { d3Scale } from 'ember-d3-helpers/helpers/d3-scale';
import { module, test } from 'qunit';

module('Unit | Helper | d3 scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Scale([42]);
  assert.ok(result);
});
