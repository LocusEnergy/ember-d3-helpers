import { d3Axis } from 'ember-d3-helpers/helpers/d3-axis';
import { module, test } from 'qunit';

module('Unit | Helper | d3 axis');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Axis([42]);
  assert.ok(result);
});
