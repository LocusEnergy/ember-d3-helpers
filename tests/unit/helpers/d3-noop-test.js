import { d3Noop } from 'ember-d3-helpers/helpers/d3-noop';
import { module, test } from 'qunit';

module('Unit | Helper | d3 noop');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Noop([42]);
  assert.ok(result);
});
