import timeseries from 'ember-d3-helpers/utils/timeseries';
import { module, test } from 'qunit';

module('Unit | Utility | timeseries');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeseries();
  assert.ok(result);
});
